<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee 
                        https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
    version="5.0">
    
    <display-name>SimplePhotoGallery</display-name>

    <!-- 1. DEFINE: GalleryServlet -->
    <servlet>
        <servlet-name>GalleryServlet</servlet-name>
        <servlet-class>com.gallery.GalleryServlet</servlet-class>
    </servlet>
    
    <!-- 2. MAPPING: Map /gallery to the GalleryServlet class -->
    <servlet-mapping>
        <servlet-name>GalleryServlet</servlet-name>
        <url-pattern>/gallery</url-pattern>
    </servlet-mapping>

    <!-- 3. DEFINE: UploadServlet -->
    <servlet>
        <servlet-name>UploadServlet</servlet-name>
        <servlet-class>com.gallery.UploadServlet</servlet-class>
    </servlet>
    
    <!-- 4. MAPPING: Map /upload to the UploadServlet class -->
    <servlet-mapping>
        <servlet-name>UploadServlet</servlet-name>
        <url-pattern>/upload</url-pattern>
    </servlet-mapping>

    <!-- 5. WELCOME FILE: Direct root (/) access to the /gallery mapping -->
    <welcome-file-list>
        <!-- This must match the URL pattern defined above -->
        <welcome-file>gallery</welcome-file>
    </welcome-file-list>
</web-app>
